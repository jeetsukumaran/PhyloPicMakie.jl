---
date-created: 2026-08-15T23:22:21-07:00
workflow-instrument: Tasking plan
workflow-status: Superseded
workflow-agent-thread-id: codex/01a00924-7ff3-7342-8c39-d0a757906710
workflow-agent-implementing-id:
  - codex/01a00951-751c-7220-9eba-1e293995fd47
workflow-location: /home/jeetsukumaran/site/storage/local/computing/research/20260414_PhyloPicMakie.jl/PhyloPicMakie.jl
workflow-production-id: diataxis-docs-primer
workflow-prd: .workflow-docs/202608140015_diataxis-docs-rewrite/01_prd.md
workflow-tranche: .workflow-docs/202608140015_diataxis-docs-rewrite/02_tranches.md
---

# Tasks for tranche 2: Write the offline primer and tutorial

## Remedial correction required: 2026-08-16 owner correction

This implemented tasking file is superseded archival context. It predates the project owner's 2026-08-16 correction that the primer and tutorial must teach the normal user trajectory: start from PhyloPic identifiers, taxon-derived identifiers, or package-supported PhyloPic lookup, then place silhouettes.

All instructions in this file that require `docs/src/_offline_silhouette.jl`, `offline_silhouette()`, preloaded image matrices, an offline tutorial as the required learning path, or exclusion of `node_uuid` from the first learning path are superseded by `.workflow-docs/202608140015_diataxis-docs-rewrite/02_tranches.md` Tranche R1 and the revised PRD. Do not execute this tasking as future guidance without first completing R1 remediation.

DataCaches guidance is also superseded: first-contact pages may hide cache setup details, but later documentation must explicitly describe repeated-query caching only where local source supports it, currently `PhyloPicDB.batch_primary_images` and `PhyloPicDB.batch_images` in `src/PhyloPicDB/_bulk.jl`.

This file also predates the repo-local `STYLE-agent-language.md` update. Any
instruction below that uses ownership, contract, boundary, layer, invariant,
compatibility, verification, source, or responsibility language is not
executable unless Tranche R1 or a regenerated tasking file expands it with the
exact file, function, module, public surface, or external contract; the
behavior; the consumer; the duplicate or bypass path that must not keep the
behavior; and the verification artifact that fails the vague statement.

## Settled user decisions and environment baseline

- This is a documentation feature. Public API behavior, exported names, plotting behavior, network behavior, source implementation, test behavior, CI behavior, and the example-script source remain out of scope.
- The primer and tutorial must be reader-facing. They must begin with figures, silhouettes, coordinates, preloaded image matrices, and saved figures, not with implementation mechanics.
- The required learning path is offline-capable. It uses the documented preloaded-image `glyph` argument to `augment_phylopic!`; it must not use `node_uuid` or a live PhyloPic request.
- Historical assignment superseded by Tranche R1: `docs/src/primer.md` provided a short first conceptual pass and `docs/src/tutorial.md` provided a guided offline learning path. That offline-first path is not current product direction.
- `src/_augment_api.jl` defines public signatures, keyword meanings, return behavior, and errors consumed by `docs/src/api/rendering.md`. The primer and tutorial must link to `docs/src/api/rendering.md` for API facts rather than duplicating its parameter catalog.
- `examples/src/explicit_overlays.jl` remains the executable evidence for the fuller offline point-and-range example. It rendered successfully during this tasking run to `/tmp/diataxis_tasking_explicit_overlays.png`, a 2360 x 1120 PNG that visibly contains the point and range panels. The task must not edit that script.
- Generated PNG files are build products and verification artifacts. `docs/build/` is ignored, and no generated PNG may be committed under `docs/src/`, `examples/`, or another source-controlled documentation-asset path.
- The current uncommitted Tranche 1 worktree changes are intentional inputs: `README.md`, `docs/make.jl`, `docs/src/index.md`, `docs/src/primer.md`, `docs/src/tutorial.md`, `docs/src/how-to/index.md`, and `docs/src/explanation/index.md`. Preserve them while replacing only the primer and tutorial route-page bodies in this tranche.
- `docs/Project.toml` currently declares only `Documenter` and `PhyloPicMakie`; it does not declare `CairoMakie`. The project owner must authorize the one docs-environment dependency addition in task 1 before task 2 runs.
- `docs/Manifest.toml` is a local ignored manifest under the repository-wide manifest policy. It may be regenerated by Pkg but must not be added to source control.
- No exact public inference, typed-return, migration, or schema guarantee is part of this tranche.
- **User-ratified execution correction (2026-08-16):** Documenter evaluates the top-level `primer.md` and `tutorial.md` examples from `docs/build/`, while the rendered route pages live in `docs/build/primer/` and `docs/build/tutorial/`. The setup blocks must therefore include `docs/src/_offline_silhouette.jl` through `joinpath(pwd(), "..", "src", "_offline_silhouette.jl")`, and the examples must explicitly save their PNGs beneath `primer/` and `tutorial/` from that build working directory. The Markdown image paths must name `primer/primer-silhouettes.png` and `tutorial/tutorial-first-figure.png` from the documentation build root; Documenter resolves them relative to their rendered route pages. This correction is limited to the Documenter path mechanics; it preserves the required final PNG paths, named example blocks, docs-only scope, and no-tracked-assets rule.

## Governance

Before implementation, read these documents line by line and comply with them throughout the work:

- `STYLE-workflow-docs.md`.
- `STYLE-makie.md`.
- `STYLE-git.md`.
- `STYLE-writing.md`.
- `STYLE-julia.md`.
- `STYLE-docs.md`.
- `STYLE-vocabulary.md`.
- `STYLE-architecture.md`.
- `STYLE-verification.md`.
- `STYLE-upstream-contracts.md`.
- `STYLE-agent-language.md`.
- `STYLE-agent-handoffs.md`.
- `STYLE-workflow-vocabulary.md`.

Apply the concrete-language rule from repo-local `STYLE-agent-language.md`: every statement that assigns a responsibility must name the exact file, function, or external contract; the behavior; the consuming surface; the forbidden duplicate or bypass; and the verification artifact.

Expected governance documents not found during tasking are repo-local `CONTRIBUTING*.md`, `STYLE-python.md`, and `STYLE-domain-vocabulary.md`. Their absence does not block this documentation-only tranche because the controlling repo-local style and vocabulary files listed above are present.

Read-only git and shell commands may be used freely. Mutating git operations, including commit, merge, push, branch, checkout, rebase, and reset, remain the human project owner's responsibility.

## Upstream primary sources and settled contract conclusions

Read these upstream primary sources in full before implementation:

- [Diataxis tutorials](https://diataxis.fr/tutorials/).
- [Documenter syntax](https://documenter.juliadocs.org/stable/man/syntax/), specifically `@meta`, `@setup`, named `@example`, generated multimedia, and build working-directory behavior.
- [Documenter public API](https://documenter.juliadocs.org/stable/lib/public/), specifically build and manual-page processing behavior.
- [CairoMakie](https://docs.makie.org/stable/explanations/backends/cairomakie.html), only after the review in task 1 authorizes the docs dependency.

Tasking conclusions from those sources:

- A Diataxis tutorial is a guided lesson. It must show the destination at the start, use a single meaningful activity, take small concrete actions, state expected results, and reserve extended explanation for links to later material.
- Documenter evaluates named `@example` blocks in a shared page-local context, writes files into the page's output directory, and allows those files to be linked from the rendered Markdown. The primer and tutorial therefore generate their own PNGs in `docs/build/primer/` and `docs/build/tutorial/`; no source asset is required.
- Documenter processes the established primer and tutorial route pages through the existing `pages` list in `docs/make.jl`. This tranche must not change that navigation.
- CairoMakie is the selected static rendering backend for the docs-only figure blocks. Its output is used only to save the two verification PNGs; it does not alter PhyloPicMakie's plotting semantics.

## Primary-goal lock

### Lock 1: Authorize a reproducible rendered-docs path

- The work is not complete if the primer and tutorial satisfy the visible-figure requirement only through a manually supplied or committed image, or if they rely on an undeclared backend that a clean docs environment cannot load.
- Direct red-state repro: `docs/Project.toml` contains only `Documenter` and `PhyloPicMakie`; `rg -n "!\\[|@example|@eval|CairoMakie" docs/src/primer.md docs/src/tutorial.md docs/Project.toml` returns no result. The current pages render only route prose.
- Task that closes it: task 1 records the 2026-08-16 project decision, and task 2 implements the approved dependency path.
- Verification artifact that must fail the old implementation or a manual-image anti-fix: `julia --project=docs docs/make.jl` must execute the named examples, and `docs/build/primer/primer-silhouettes.png` plus `docs/build/tutorial/tutorial-first-figure.png` must exist as PNGs after the build. A committed `docs/src/**/*.png` does not satisfy this lock.

### Lock 2: Show a user-centered primer result

- The work is not complete if `docs/src/primer.md` remains a route page, or if it introduces PhyloPicMakie with internal overlay, projection, anchor-space, or ownership language before showing a reader-visible figure.
- Direct red-state repro: the current primer says, "Use this primer route" and "The primer will gather," contains no rendered image or executable block, and links readers away before explaining an offline first result.
- Task that closes it: task 3.
- Verification artifact that must fail the old implementation or a text-only passing implementation: `docs/build/primer/index.html` must reference `primer-silhouettes.png`; `file docs/build/primer/primer-silhouettes.png` must report PNG image data; visual review must show silhouettes at plotted coordinates; and the old route-page phrases must be absent from `docs/src/primer.md`.

### Lock 3: Provide a coherent offline tutorial

- The work is not complete if a new reader cannot follow one page from imports through a saved silhouette-annotated figure without live network access or knowledge of the internal rendering implementation.
- Direct red-state repro: the current tutorial says, "Use this tutorial route," "The current examples are runnable now," and "This route will connect," but contains no ordered exercise, expected results, saved output, or executable documentation example.
- Task that closes it: task 4.
- Verification artifact that must fail the old implementation or a code-list anti-fix: `docs/build/tutorial/index.html` must reference `tutorial-first-figure.png`; `file docs/build/tutorial/tutorial-first-figure.png` must report PNG image data; `rg -n "node_uuid|HTTP|Downloads|network" docs/src/tutorial.md` must return no match; and rendered-page review must show the ordered lesson and its expected result notes.

### Lock 4: Keep runnable offline evidence real

- The work is not complete if the tutorial figure makes claims that the existing offline example no longer demonstrates, or if verification stops at a documentation build without testing the associated executable example artifact.
- Direct red-state repro: `docs/src/examples.md` currently lists `explicit_overlays.jl` as a command. A passing docs build does not prove that this script wrote a visible PNG.
- Tasks that close it: task 3 links to `examples.md`; task 4 links to the fuller offline example; task 5 runs and inspects `explicit_overlays.jl` without changing it.
- Verification artifact that must fail the old documentation-only passing implementation: `julia --project=examples examples/src/explicit_overlays.jl /tmp/explicit_overlays.png` must succeed and `file /tmp/explicit_overlays.png` must report PNG image data. The tasking revalidation already proved the same command shape with a tasking-specific temporary filename.

### Lock 5: Preserve Diataxis roles and project vocabulary

- The work is not complete if the primer becomes an API catalog, the tutorial becomes an explanation essay, or either page reintroduces the first-contact developer babble rejected in the PRD.
- Direct red-state repro: the current pages defer to API reference and examples instead of providing their assigned Diataxis role. The earlier documentation state used phrases including "generic anchored-overlay substrate," "owner layer," and "projected pixel-anchor placement mechanics."
- Tasks that close it: tasks 3, 4, and 5.
- Verification artifact that must fail a weak prose-only implementation: source audits in task 5 reject the old route-page phrases, forbidden implementation-first phrases, `@autodocs`, and `@index` in the primer and tutorial. Rendered review verifies that the primer points to the tutorial and that the tutorial links to the reference rather than copying a parameter catalog.

### Lock 6: Preserve the documentation-only boundary

- The work is not complete if it changes PhyloPicMakie source code, public behavior, tests, example-script source, CI, navigation, or commits a generated PNG to make the pages easier to write.
- Direct red-state repro: the PRD explicitly forbids implementation changes made only to simplify explanation. The current worktree also contains uncommitted Tranche 1 changes that this tranche must preserve rather than overwrite.
- Tasks that close it: every task has an explicit scope boundary; task 5 audits the complete diff.
- Verification artifact that must fail a scope-expanding anti-fix: `git diff --name-only` may contain only the existing Tranche 1 documentation-map paths, this workflow directory, `docs/Project.toml`, `docs/src/_offline_silhouette.jl`, `docs/src/primer.md`, and `docs/src/tutorial.md`. `git status --short --ignored docs/build docs/Manifest.toml` must show the build directory and local manifest as ignored rather than newly tracked files.

## Forbidden passing implementation table

| Lock item | Required behavior | Current code state | Resolved implementation instruction | Forbidden passing implementation | Failing verification artifact |
| --- | --- | --- | --- | --- | --- |
| Lock 1: Authorize a reproducible rendered-docs path | The docs environment declares and loads the static renderer that creates page-local build PNGs. | `docs/Project.toml` has `Documenter` and `PhyloPicMakie` only; no current primer or tutorial block creates an image. | Task 1 asks the owner to authorize a direct `CairoMakie` docs dependency. Task 2 runs `Pkg.add("CairoMakie")` in `docs/` and keeps the regenerated ignored manifest untracked. | Copy `/tmp/explicit_overlays.png` into `docs/src/`, link it in Markdown, and leave `CairoMakie` undeclared in the docs project. | The docs build must create both PNGs in `docs/build/`; `git ls-files docs/src | rg "\\.png$"` must return no result. |
| Lock 2: Show a user-centered primer result | The primer opens with an offline, rendered silhouette figure and a concise explanation of coordinates plus preloaded image matrices. | `docs/src/primer.md` is a short route page with no executable block or image. | Task 3 replaces the route body, adds `docs/src/_offline_silhouette.jl`, and uses a named Documenter example that saves `primer-silhouettes.png` in the primer build directory. | Add only a static code fence, describe the expected figure in prose, or use a different unverified image while retaining a text-only primer. | `rg -n "primer-silhouettes\\.png" docs/build/primer/index.html`; `file docs/build/primer/primer-silhouettes.png`; and manual image review fail the static-code or missing-image form. |
| Lock 3: Provide a coherent offline tutorial | The tutorial is an ordered lesson that creates and saves one silhouette-annotated figure without live data. | `docs/src/tutorial.md` contains route prose only and no ordered activity or saved artifact. | Task 4 replaces the route body with named Documenter example blocks that share the task-3 helper, construct one figure in small steps, save `tutorial-first-figure.png`, and link to reference for options. | Rename the current route page as a tutorial, paste the full API signature, or tell users to run the separate example without giving a guided workflow. | `rg -n "tutorial-first-figure\\.png" docs/build/tutorial/index.html`; `file docs/build/tutorial/tutorial-first-figure.png`; the no-live-input source audit; and rendered lesson review fail those incomplete forms. |
| Lock 4: Keep runnable offline evidence real | The documented fuller offline example continues to save a visible PNG under its examples environment. | `examples/src/explicit_overlays.jl` contains the point and range panel, but docs currently provide only a command list and the script is not part of the docs build. | Task 5 runs the unmodified script to `/tmp/explicit_overlays.png` and validates the output type after the docs build. | Treat the docs-generated tutorial image as a substitute for executing `explicit_overlays.jl`, or modify the script only to make verification superficially pass. | The script command and `file /tmp/explicit_overlays.png` fail when the script cannot produce the artifact; the diff audit rejects script edits. |
| Lock 5: Preserve Diataxis roles and project vocabulary | The primer gives a quick conceptual first result; the tutorial gives a lesson; precise API details remain linked reference material. | The pages are routes, while the rendering reference contains internal-first prose and `@autodocs`. | Tasks 3 and 4 restrict their prose to reader-visible results, concrete actions, and expected observations, then link to `api/rendering.md` and `examples.md`. | Put `@autodocs`, a copied keyword table, internal overlay wording, or an extended mechanics explanation into either teaching page. | `rg -n "@autodocs|@index|generic anchored-overlay|shared internal anchored-overlay substrate|owner layer|data-anchor|projected pixel-anchor|placement mechanics|projection mechanics|internal owner" docs/src/primer.md docs/src/tutorial.md` must return no match; rendered review confirms the page roles. |
| Lock 6: Preserve the documentation-only boundary | Only the approved docs project configuration and primer/tutorial support surfaces change; no generated source asset is tracked. | The worktree already has uncommitted Tranche 1 documentation-map files. The PRD excludes implementation, example-source, CI, navigation, and public behavior changes. | Tasks 2 through 4 name the sole new or changed Tranche 2 files. Task 5 checks the complete diff against the listed union of Tranche 1 and Tranche 2 paths. | Alter `src/_augment_api.jl`, `examples/src/explicit_overlays.jl`, `.github/workflows/CI.yml`, `docs/make.jl`, or a public API docstring to avoid documenting existing behavior. | `git diff --name-only` and `git status --short --ignored docs/build docs/Manifest.toml` fail the scope-expanding or asset-tracking form. |

## Handoff packet

- **Active authorities**: the parent PRD, parent tranche plan, this tasking file, all repo-local governance files named in Governance, and repo-local `STYLE-agent-language.md`, `STYLE-agent-handoffs.md`, and `STYLE-workflow-vocabulary.md`.
- **Parent documents**: `.workflow-docs/202608140015_diataxis-docs-rewrite/01_prd.md` and `.workflow-docs/202608140015_diataxis-docs-rewrite/02_tranches.md`.
- **Settled decisions and non-negotiables**: Diataxis structure remains fixed; the required tutorial is offline; generated PNGs are build products; public API and implementation behavior cannot change; the current Tranche 1 navigation work remains intact; `CairoMakie` cannot enter the docs environment until task 1 approves it.
- **Authorization boundary**: task 2 may update `docs/Project.toml`; task 3 may update `docs/src/primer.md` and create `docs/src/_offline_silhouette.jl`; task 4 may update `docs/src/tutorial.md`; task 5 edits nothing. No other source file or user-facing public contract is in scope.
- **Current-state diagnosis**: Tranche 1 has installed route pages and navigation. The primer and tutorial remain the deliberately minimal route pages. The docs environment lacks CairoMakie. The offline example script renders a concrete point-and-range PNG with no network data.
- **Primary-goal lock**: preserve locks 1 through 6 as separate completion requirements.
- **Direct red-state repros**: read the current primer and tutorial; inspect `docs/Project.toml`; run the document source audit in task 5; run the existing offline example to a temporary PNG.
- **Responsible entities and behaviors**: `docs/Project.toml` declares docs-only render dependencies; `docs/src/_offline_silhouette.jl` supplies one minimal in-memory image helper to the two Documenter examples; `docs/src/primer.md` creates the first rendered concept figure; `docs/src/tutorial.md` creates the ordered first-figure lesson; Documenter builds the local PNGs; `examples/src/explicit_overlays.jl` provides the fuller point-and-range evidence. No other file may duplicate those responsibilities.
- **Exact files in scope**: `docs/Project.toml`; `docs/src/_offline_silhouette.jl`; `docs/src/primer.md`; `docs/src/tutorial.md`; ignored build products under `docs/build/`; ignored `docs/Manifest.toml` regenerated by Pkg.
- **Exact files and surfaces out of scope**: `README.md`; `docs/make.jl`; `docs/src/index.md`; `docs/src/examples.md`; `docs/src/how-to/`; `docs/src/explanation/`; `docs/src/api/`; `src/`; `test/`; `examples/src/`; `examples/README.md`; `.github/workflows/CI.yml`; public API signatures; exported names; plotting behavior; network behavior; source-controlled PNG assets.
- **Required upstream primary sources**: the Diataxis, Documenter, and CairoMakie pages named above.
- **Green-state gates**: task 1 owner approval; a fresh docs build; two docs-build PNG type checks; the unmodified explicit-overlay script PNG type check; the existing CI-style doctest command; source and rendered reviews; and the diff-plus-ignored-assets audit.
- **Stop conditions**: stop before task 2 when the owner declines the direct CairoMakie docs dependency; stop when a required image cannot be generated in `docs/build/`; stop when documentation would require a public API change, a live request, or an example-script change; stop when an upstream contract required for the build has not been read.

## Required revalidation before implementation

- Read the parent PRD, parent tranche plan, and this tasking file in full.
- Read every governance document named in Governance line by line.
- Read the cited Diataxis, Documenter, and CairoMakie primary sources in full before their constrained task begins.
- Re-read `docs/Project.toml`, `docs/make.jl`, `docs/src/index.md`, `docs/src/primer.md`, `docs/src/tutorial.md`, `docs/src/examples.md`, `docs/src/api/rendering.md`, `src/_augment_api.jl`, `src/_render_core.jl`, and `examples/src/explicit_overlays.jl` in full.
- Re-run `julia --project=docs docs/make.jl` and inspect the route-only primer and tutorial before editing them.
- Re-run the explicit-overlay script to a temporary PNG and inspect it before writing prose about its visible result.
- Stop and raise the discrepancy when the code, outputs, or user authorization no longer match this handoff.

## Tranche execution rule

This tranche may replace the temporary primer and tutorial route prose with rendered, offline teaching pages after the task-1 review. It must begin and end with the docs build, existing doctest gate, and offline script evidence green.

`docs/src/primer.md` must stop being a route-only page. `docs/src/tutorial.md` must stop being a route-only page. Neither page may become a second API reference, a general explanation page, a live-data guide, or a new implementation owner. `docs/src/_offline_silhouette.jl` may supply only the small in-memory image used by both docs examples; it must not become an alternate public rendering API or a duplicate of `examples/src/explicit_overlays.jl`.

## Non-negotiable execution rules

- Do not continue past task 1 without the owner approval recorded by that task.
- Do not edit package `Project.toml`, package source, test source, example-script source, CI, navigation, API reference pages, or the existing Tranche 1 documentation-map files.
- Do not copy a generated PNG into `docs/src/` or commit one anywhere in the repository.
- Do not add a network-dependent `node_uuid` example, `Downloads` call, HTTP call, or remote fetch to the primer or tutorial.
- Do not use `@autodocs`, `@index`, a copied full signature, or a keyword catalog in the primer or tutorial.
- Do not put source-text policing into CI or tests. The source audits in this tasking are session verification artifacts for the documented reader experience.
- Do not describe internal overlay implementation, data-anchor behavior, projected pixel-anchor behavior, owner layers, or projection mechanics in either first-contact page.
- Do not overwrite or discard the current uncommitted Tranche 1 changes.

## Concrete anti-patterns or removal targets

- Remove the temporary primer sentences beginning "Use this primer route" and "The primer will gather."
- Remove the temporary tutorial sentences beginning "Use this tutorial route" and "This route will connect."
- Prevent a static code fence from surviving as the only first-figure evidence.
- Prevent a checked-in `docs/src` image from replacing a build-generated Documenter output.
- Prevent `docs/src/_offline_silhouette.jl` from accumulating point, range, label, gallery, or image-source logic already demonstrated by `examples/src/explicit_overlays.jl`.
- Prevent the primer and tutorial from copying `src/_augment_api.jl` docstring tables or internal implementation prose instead of linking to `docs/src/api/rendering.md`.

## Failure-oriented verification

- Build the docs and require `docs/build/primer/primer-silhouettes.png` and `docs/build/tutorial/tutorial-first-figure.png` to be PNG files. A page with code but no generated image fails this check.
- Search the new primer and tutorial for the stale route-page language, `node_uuid`, live-data imports, `@autodocs`, `@index`, and the forbidden implementation-first phrases. A page that builds but retains any of those forms fails this check.
- Open both generated images and both rendered HTML pages. The primer must show a visible offline coordinate figure before its reference link. The tutorial must show an ordered lesson with expected-result notes and its saved figure.
- Run the unmodified `examples/src/explicit_overlays.jl` to `/tmp/explicit_overlays.png` and validate its PNG file type. A green docs build alone does not close the executable-evidence lock.
- Run the CI-style doctest command after the docs build. A docs-generated `@example` does not replace the existing docstring gate.
- Audit the full diff and ignored artifacts. A code change, navigation change, tracked manifest, or tracked PNG fails the authorization-boundary lock.

## Tasks

### 1. Approve the docs-only rendering dependency

**Type**: REVIEW
**Output**: The project owner records either approval or rejection of one direct `CairoMakie` dependency in `docs/Project.toml`, used solely by Documenter examples to emit ignored build-tree PNGs.
**Depends on**: none.
**Positive contract**: Approval authorizes exactly `CairoMakie` in the docs environment, named `@example` blocks in the primer and tutorial, and two images created under `docs/build/` during `julia --project=docs docs/make.jl`. Rejection stops Tranche 2 because a source-controlled static PNG and an undeclared backend are both forbidden.
**Negative contract**: This decision does not authorize changes to the package environment, public API, examples environment, example-script source, CI, navigation, source-controlled images, or live-data behavior.
**Files**: No repository file may be changed by this task.
**Out of scope**: Every project file and every implementation decision other than the single docs-only CairoMakie authorization.
**Verification**: Confirm the decision states the exact dependency name, direct docs-environment scope, build-only PNG location, and prohibition on tracked image assets. The decision must reject an implied approval inferred from the examples environment.

The review is required because the approved PRD permits documentation-build wiring but reserves documentation-environment dependency changes for the project owner. The current docs environment cannot execute a static render: `docs/Project.toml` lacks `CairoMakie`, while `examples/Project.toml` uses CairoMakie only in the separate examples environment. The recommended decision is approval because Documenter can create page-local multimedia artifacts from `@example` blocks and CairoMakie provides the headless static backend. Record the exact answer in the implementation report before task 2 begins.

### 2. Add the approved static renderer to the docs environment

**Type**: CONFIG
**Output**: `docs/Project.toml` declares `CairoMakie` directly, and a fresh docs environment can load `CairoMakie`, `Documenter`, and `PhyloPicMakie` together.
**Depends on**: task 1 approval.
**Positive contract**: Run `Pkg.add("CairoMakie")` from the docs project through Julia's Pkg interface. The tracked project file declares the dependency; the local ignored manifest resolves it; and the direct import succeeds before any teaching page references the backend.
**Negative contract**: Do not manually edit a UUID into `docs/Project.toml`. Do not alter the root, test, or examples environments. Do not track `docs/Manifest.toml`, add a generated image, or change the docs build configuration.
**Files**: `docs/Project.toml`; ignored `docs/Manifest.toml` may be regenerated locally.
**Out of scope**: `Project.toml`; `test/Project.toml`; `examples/Project.toml`; `docs/make.jl`; all Markdown pages; `src/`; `test/`; `examples/`; `.github/`.
**Verification**: Run `julia --project=docs -e 'using CairoMakie; using Documenter; using PhyloPicMakie'`; run `git diff -- docs/Project.toml`; run `git check-ignore -q docs/Manifest.toml`; and run `git ls-files docs/Manifest.toml` with no output.

Use Pkg because `STYLE-julia.md` forbids dependency changes by hand-edited UUID. CairoMakie is the only new direct dependency. It supplies static image saving inside Documenter's page-local working directory; the package API remains unchanged.

### 3. Replace the primer route with an offline rendered first result

**Type**: WRITE
**Output**: The primer contains a concise reader-facing first result, a rendered build-only figure at `docs/build/primer/primer-silhouettes.png`, and links to the tutorial, fuller example, and API reference.
**Depends on**: task 2.
**Positive contract**: Create `docs/src/_offline_silhouette.jl` with one small, docs-private `offline_silhouette` helper that returns an in-memory transparent RGBA image. Replace `docs/src/primer.md` with a short primer that loads that helper in a hidden named `@setup` block and uses a named `@example` block to create a CairoMakie coordinate figure with preloaded images through the public `augment_phylopic!` `glyph` argument. The example saves `primer-silhouettes.png` in Documenter's primer output directory and the Markdown embeds that file. The prose explains that coordinates mark where silhouettes go and that a preloaded image matrix supplies the silhouette. It links to `tutorial.md`, `examples.md`, and `api/rendering.md`.
**Negative contract**: Do not use `node_uuid`, HTTP, Downloads, a live PhyloPic request, `@autodocs`, `@index`, internal overlay terminology, a copied API keyword table, a source-controlled PNG, or a duplicate of the fuller point-and-range script. Do not edit `docs/make.jl`, `docs/src/index.md`, the examples page, API pages, or the existing example script.
**Files**: `docs/src/_offline_silhouette.jl`; `docs/src/primer.md`.
**Out of scope**: `docs/Project.toml`; `docs/make.jl`; `docs/src/tutorial.md`; every other Markdown page; `src/`; `test/`; `examples/`; `.github/`; generated images under `docs/build/` as tracked files.
**Verification**: Run `julia --project=docs docs/make.jl`; run `test -f docs/build/primer/primer-silhouettes.png`; run `file docs/build/primer/primer-silhouettes.png`; run `rg -n "primer-silhouettes\\.png" docs/build/primer/index.html`; run `rg -n "Use this primer route|The primer will gather|node_uuid|HTTP|Downloads|@autodocs|@index|generic anchored-overlay|shared internal anchored-overlay substrate|owner layer|data-anchor|projected pixel-anchor|placement mechanics|projection mechanics|internal owner" docs/src/primer.md` and require no match; then open the generated PNG for visual review.

Use the existing `@meta CurrentModule = PhyloPicMakie` pattern. Name the setup and example blocks `primer-figure` so their state is shared only within the primer page. Load the helper using a path built from Documenter's documented page-output working directory: from `docs/build/primer/`, traverse to `docs/src/_offline_silhouette.jl`. The helper creates only the small in-memory image used in the primer and tutorial; it must not construct a figure, save a file, or call the public plotting API. The primer's named example creates the figure, calls `CairoMakie.save` with the fixed build-relative filename, and leaves the generated image as the page's build product. Use one compact example and a short expected-result note. The Diataxis tutorial page provides the guided sequence.

### 4. Replace the tutorial route with the first offline figure lesson

**Type**: WRITE
**Output**: The tutorial is a single ordered offline lesson that creates and saves `docs/build/tutorial/tutorial-first-figure.png` through an executable Documenter example.
**Depends on**: task 3.
**Positive contract**: Replace `docs/src/tutorial.md` with a lesson that starts by naming the completed figure, then guides the reader through importing CairoMakie and PhyloPicMakie, creating an axis and point coordinates, obtaining the preloaded image from task 3's helper, calling public `augment_phylopic!` with `glyph`, and saving the figure. Use small numbered sections, a visible expected-result note after each action, first-person-plural tutorial language, and a final note that links to `examples.md` for the fuller point-and-range panel plus `api/rendering.md` for all keyword details. Use a hidden named setup block and named example blocks with the shared name `first-figure`; save `tutorial-first-figure.png` from the final example and embed it in the page.
**Negative contract**: Do not mention or execute `node_uuid`, image download, HTTP, a network request, range placement, thumbnail galleries, GraphMakie, PBDB, internal overlay mechanics, `@autodocs`, `@index`, or a copied full API catalog. Do not edit the primer helper, docs navigation, examples page, API pages, source implementation, or the explicit-overlay script.
**Files**: `docs/src/tutorial.md`.
**Out of scope**: `docs/Project.toml`; `docs/src/_offline_silhouette.jl`; `docs/src/primer.md`; `docs/make.jl`; all other documentation pages; `src/`; `test/`; `examples/`; `.github/`; generated images under `docs/build/` as tracked files.
**Verification**: Run `julia --project=docs docs/make.jl`; run `test -f docs/build/tutorial/tutorial-first-figure.png`; run `file docs/build/tutorial/tutorial-first-figure.png`; run `rg -n "tutorial-first-figure\\.png" docs/build/tutorial/index.html`; run `rg -n "Use this tutorial route|This route will connect|node_uuid|HTTP|Downloads|network|@autodocs|@index|generic anchored-overlay|shared internal anchored-overlay substrate|owner layer|data-anchor|projected pixel-anchor|placement mechanics|projection mechanics|internal owner" docs/src/tutorial.md` and require no match; then open the generated PNG and rendered tutorial page for review.

The lesson must use the helper path resolved from Documenter's tutorial page output directory to `docs/src/_offline_silhouette.jl`; this gives both pages one minimal image definition without copying drawing logic. State that the figure requires only local code and the installed packages. Show a completed figure before the longer instructions, then retain the same example state across the small steps so a reader sees one coherent activity rather than disconnected snippets. Each step must tell the reader what they should observe. Explain neither rendering internals nor alternate keyword choices: the next how-to and explanation tranches own those topics.

### 5. Verify rendered teaching, offline evidence, and scope

**Type**: TEST
**Output**: The docs build, existing doctest gate, two docs-generated PNGs, offline script evidence, prose audit, rendered review, and diff audit all pass.
**Depends on**: tasks 2, 3, and 4.
**Positive contract**: The docs build executes both named examples and produces the primer plus tutorial PNGs in their build directories. The existing explicit-overlay script still writes a visible PNG when run under the examples environment. The primer is a concise conceptual first pass, the tutorial is an ordered lesson, and all changed paths stay within the approved union of existing Tranche 1 and current Tranche 2 documentation files.
**Negative contract**: Do not edit files in this task. Do not respond to a failure by weakening an audit, adding source-text CI tests, checking in an image, changing examples, changing public behavior, or changing navigation. Return a failure to the task responsible for its allowed file.
**Files**: No file edits are allowed. Generated `/tmp/explicit_overlays.png`, ignored `docs/build/` outputs, and ignored `docs/Manifest.toml` are verification artifacts only.
**Out of scope**: Every source edit; CI changes; new tests; tracked image assets; public API, implementation, example-script, environment, navigation, or network-behavior changes.
**Verification**: Run `julia --project=docs docs/make.jl`; run `julia --project=docs -e 'using Documenter: DocMeta, doctest; using PhyloPicMakie; DocMeta.setdocmeta!(PhyloPicMakie, :DocTestSetup, :(using PhyloPicMakie); recursive=true); doctest(PhyloPicMakie)'`; run `file docs/build/primer/primer-silhouettes.png docs/build/tutorial/tutorial-first-figure.png`; run `julia --project=examples examples/src/explicit_overlays.jl /tmp/explicit_overlays.png && file /tmp/explicit_overlays.png`; run the primer and tutorial source audits from tasks 3 and 4; inspect `docs/build/primer/index.html` and `docs/build/tutorial/index.html` for their respective image links; inspect the two docs images and `/tmp/explicit_overlays.png`; run `git diff --name-only | sort`; run `git status --short --ignored docs/build docs/Manifest.toml`; and run `git ls-files docs/src | rg "\\.png$"` with no output.

Treat the docs build as necessary but insufficient. The old route pages can build, so the generated image checks, source audits, rendered page review, and actual examples-script artifact are mandatory. The diff audit accepts only the existing Tranche 1 files, this workflow directory, and the four Tranche 2 source files named in the handoff packet. The docs build directory and local manifest must remain ignored. Record failed commands and their owning task rather than performing out-of-scope repairs.
